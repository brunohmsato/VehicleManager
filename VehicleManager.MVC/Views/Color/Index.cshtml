@model List<VehicleManager.Domain.Models.Color>

<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="https://cdn.datatables.net/2.0.5/css/dataTables.dataTables.css" />
</head>
<body>
    <div><h2>Cadastro de Cor</h2></div>
    <hr />

    <div class="row">
        <div class="col-md-12">
            <div>
                <div class="pull-left">
                    <a asp-action="Add" class="btn btn-success">Cadastrar</a>
                </div>
            </div>
        </div>
    </div>

    <div class="panel panel-default">
        <table id="colorTable" class="table table-striped">
            <thead>
                <tr>
                    <th>Descrição</th>
                    <th>Status</th>
                    <th>#</th>
                    <th>#</th>
                </tr>
            </thead>

            <tbody>
                @foreach (var color in Model)
                {
                    <tr>
                        <td>@color.Description</td>
                        <td>@(color.Status ? "Ativo" : "Inativo")</td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@color.Id" class="btn btn-warning">Editar</a>
                        </td>
                        <td>
                            <a asp-action="Delete" asp-route-id="@color.Id" class="btn btn-danger">Excluir</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <button onclick="window.location.href = '/Vehicle/Index';" class="arrow-R">
        <img width="50" height="50" src="https://img.icons8.com/ios/50/circled-left-2.png" alt="circled-left-2" />
    </button>   
    <button onclick="window.location.href = '/Fuel/Index';" class="arrow-L">
        <img width="50" height="50" src="https://img.icons8.com/ios/50/circled-left-2.png" alt="circled-left-2" />
    </button>

</body>
</html>

@section scripts {
    <script src="https://code.jquery.com/jquery-3.7.1.js"></script>
    <script src="https://cdn.datatables.net/2.0.5/js/dataTables.js"></script>
    <script>
        new DataTable('#colorTable', {
            layout: {
                bottomEnd: {
                    paging: {
                        boundaryNumbers: false
                    }
                }
            }
        });
    </script>
}